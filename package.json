{
	"name": "pi-rakit-monorepo",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"packageManager": "npm@11.6.0",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"check": "npm run manifest:check && npm run license:check && npm test && npm run pack:dry",
		"license:check": "node scripts/sync-workspace-license.mjs --check",
		"license:sync": "node scripts/sync-workspace-license.mjs",
		"manifest:check": "node scripts/sync-installer-manifest.mjs --check",
		"manifest:sync": "node scripts/sync-installer-manifest.mjs",
		"pack:dry": "npm pack --dry-run --workspace apps/installer && npm pack --dry-run --workspace packages/hello-pi && npm pack --dry-run --workspace packages/custom-provider && npm pack --dry-run --workspace packages/doctor && npm pack --dry-run --workspace packages/worktree && npm pack --dry-run --workspace packages/git && npm pack --dry-run --workspace packages/biome && npm pack --dry-run --workspace packages/token-speed && npm pack --dry-run --workspace packages/session-usage && npm pack --dry-run --workspace packages/session-stats && npm pack --dry-run --workspace packages/session-delete && npm pack --dry-run --workspace packages/compact-tools && npm pack --dry-run --workspace packages/auto-title && npm pack --dry-run --workspace packages/onboarding && npm pack --dry-run --workspace packages/playwright-browser",
		"publish:npm": "node scripts/publish-npm.mjs",
		"publish:npm:dry": "node scripts/publish-npm.mjs --dry-run",
		"test": "node --test"
	},
	"engines": {
		"node": ">=20"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/4nndmw/pi-rakit.git"
	},
	"homepage": "https://github.com/4nndmw/pi-rakit#readme",
	"bugs": {
		"url": "https://github.com/4nndmw/pi-rakit/issues"
	}
}
