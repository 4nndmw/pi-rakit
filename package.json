{
	"name": "pi-rakit-monorepo",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"packageManager": "npm@11.6.0",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"check": "npm run manifest:sync && npm test && npm run pack:dry",
		"manifest:sync": "node scripts/sync-installer-manifest.mjs",
		"pack:dry": "npm pack --dry-run --workspace apps/installer && npm pack --dry-run --workspace packages/hello-pi && npm pack --dry-run --workspace packages/custom-provider && npm pack --dry-run --workspace packages/doctor",
		"publish:npm": "node scripts/publish-npm.mjs",
		"publish:npm:dry": "node scripts/publish-npm.mjs --dry-run",
		"test": "node --test"
	},
	"engines": {
		"node": ">=20"
	}
}
