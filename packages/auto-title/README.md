# Pi Rakit Auto Title

Automatically names new Pi sessions from the first prompt so `/resume` is easier to browse.

```bash
pi install npm:pi-rakit-auto-title
```

Titles are generated locally and deterministically: no additional model request or network call is made. Existing user-defined session names are never overwritten.
